// Function for clicking on + Folder button
const addFolder = document.getElementById(add-folder-btn);

addFolder.addEventListener("click", () => {
    const newFolder = document.createElement("div");
});